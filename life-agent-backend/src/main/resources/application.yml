spring:
  application:
    name: life-agent-backend

# 服务器配置
server:
  port: 8080
  servlet:
    context-path: /

# 日志配置
logging:
  level:
    com.dualtracklife.lifeagent: INFO
    org.springframework.web: INFO

